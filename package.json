{
  "name": "modern-gallery",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "sass": "sass src/styles/main.scss dist/styles.css",
    "sass:watch": "sass --watch src/styles/main.scss dist/styles.css"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.89.0",
    "vite": "^6.1.0"
  }
}
